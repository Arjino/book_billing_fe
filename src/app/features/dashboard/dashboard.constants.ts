/** Tuning defaults for `DashboardService.getDashboardOverview()`. */

/** Book stock at or below this many units is flagged as low/out of stock. */
export const DEFAULT_LOW_STOCK_THRESHOLD = 20;

/** Number of rows shown in the "Recent Sales Invoices" table by default. */
export const DEFAULT_RECENT_INVOICES_LIMIT = 5;

/** Number of rows shown in the "Top Outstanding Parties" panel by default. */
export const DEFAULT_TOP_DEBTORS_LIMIT = 4;

/** Number of rows shown in the "Low Stock Inventory Alert" panel before collapsing behind "+N more". */
export const DEFAULT_LOW_STOCK_ALERTS_LIMIT = 5;
