/** Tuning defaults for `BusinessAnalyticsService.getOverview()`. */

/** Book stock at or below this many units is flagged as low/out of stock. */
export const ANALYTICS_LOW_STOCK_THRESHOLD = 20;

/** Max rows shown in the "Top Selling Book Titles" leaderboard. */
export const TOP_SELLING_BOOKS_LIMIT = 5;

/** Max rows shown in the "Revenue by Publisher" breakdown. */
export const TOP_PUBLISHERS_LIMIT = 5;

/** Max rows shown in the "Outstanding Client Dues" leaderboard. */
export const TOP_DEBTORS_LIMIT = 5;

export const WEEKDAY_LABELS = ['Sun', 'Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat'];
